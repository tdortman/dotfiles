# Your patch layer for this dsh profile, applied after every bundle layer:
# a top-level YAML array of loader patch entries (id-targeted config
# overrides, disables, and insert lists; `!!js` expressions allowed).
- id: llm-openai-codex
  config:
    searchModel: gpt-5.6-luna
    searchMode: live
    searchContextSize: high

# dsh-opencode-session defaults to the pi-ai catalog route keys (`opencode`,
# `opencode-go`). This profile serves OpenCode Go under its own route key, so
# restate the whole `config` with that key added.
- id: opencode-go-session-header
  config:
    providers:
      - opencode-goo
      - opencode-goo-deepseek
    mode: session-id
    debug: false
